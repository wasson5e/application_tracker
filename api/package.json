{
  "name": "job-application-tracker-api",
  "version": "1.0.0",
  "description": "REST API for the Job Application Tracker",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "test:unit": "jest tests/unit --runInBand",
    "test:property": "jest tests/property --runInBand --testTimeout=30000",
    "test:integration": "jest tests/integration --runInBand --testTimeout=60000"
  },
  "dependencies": {
    "dotenv": "16.4.5",
    "express": "4.19.2",
    "mongodb": "6.7.0",
    "morgan": "1.10.0"
  },
  "devDependencies": {
    "fast-check": "3.20.0",
    "jest": "29.7.0",
    "testcontainers": "^12.0.4"
  }
}
